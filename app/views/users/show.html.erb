<h1>Welcome to Team Tracker <%= @user.name %>.</h1><br>

<% @user.teams.each do |team| %>
    League: <%= link_to team.league.name, league_path(team.league) %></br>
    Team: <%= link_to "#{team.city} #{team.name}", user_team_path(team.user, team)%>
<% end %>

<br>
<br>

<%= link_to "Log Out", logout_path %>