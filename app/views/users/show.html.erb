<h1>Welcome to Team Tracker <%= @user.name %>.</h1><br>

<% @user.teams.each do |team| %>
    <h2>League: <%= link_to team.league.name, league_path(team.league) %></h2>
    <h3>Team: <%= link_to "#{team.city} #{team.name}", user_team_path(team.user, team)%></h3><br><br>
<% end %>
