<% if object.errors.any? %>
    <div id="error_explanation">
        <h2>
            <%= pluralize(object.errors.count, "error") %>
            prohibited this from being saved:
        </h2>
         
        <ul>
        <% object.errors.full_messages.each do |msg| %>
            <li><div class="error"><%= msg %></div></li>
        <% end %>
        </ul>
    </div>
<% end %>