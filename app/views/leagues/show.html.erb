<h1><%= @league.name %></h1>
<% @league.teams.each do |team| %>
    <h2><%= link_to city_and_name(team), user_team_path(team.user, team) %></h2>
<% end %>

<%= link_to "Home", user_path(current_user) %><br><br>

<%= link_to "Log Out", logout_path %>